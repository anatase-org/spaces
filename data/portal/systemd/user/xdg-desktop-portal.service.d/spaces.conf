[Unit]
# The Spaces public-portal router owns org.freedesktop.portal.Desktop.  Keep a
# distro-provided frontend installed for its tools and XML, but never start it
# in a Space session.
ConditionEnvironment=!SPACES_NAME
